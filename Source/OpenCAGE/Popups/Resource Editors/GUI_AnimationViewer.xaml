<UserControl x:Class="OpenCAGE.Popups.UserControls.GUI_AnimationViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:HelixToolkit="clr-namespace:HelixToolkit.Wpf;assembly=HelixToolkit.Wpf"
             xmlns:local="clr-namespace:OpenCAGE.Popups.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <HelixToolkit:HelixViewport3D x:Name="myView" ZoomExtentsWhenLoaded="False" ShowViewCube="True" ViewCubeVerticalPosition="Top" ShowCoordinateSystem="True" CoordinateSystemVerticalPosition="Top">
            <HelixToolkit:SunLight Altitude="160" Azimuth="120" Ambient="0.7" Brightness="1" />
            <ModelVisual3D x:Name="posedModel"/>
            <ModelVisual3D x:Name="posedSkeleton"/>
            <!-- The mesh is Y up, and GridLinesVisual3D defaults to the XY plane, which stands the
                 floor on its edge. Normal and LengthDirection lay it flat under the character. -->
            <HelixToolkit:GridLinesVisual3D Width="4" Length="4" MinorDistance="0.25" MajorDistance="1"
                                            Thickness="0.004" Fill="#22808080"
                                            Normal="0,1,0" LengthDirection="1,0,0" Center="0,0,0"/>
        </HelixToolkit:HelixViewport3D>
    </Grid>
</UserControl>
