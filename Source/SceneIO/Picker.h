#pragma once
#include "Utilities.h"

/* A picker that can generate a ray from the viewport through the scene for selecting objects */
static class Picker {
public:
	static Ray GenerateRay();

private:

};